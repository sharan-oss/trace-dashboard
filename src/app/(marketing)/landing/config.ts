/*
 * Landing page live values — SHARAN EDITS THESE.
 *
 * SLOTS_LEFT_THIS_MONTH must stay TRUE (spec requirement: it renders as
 * "Only N slots left this month" in the final CTA). Update it whenever a
 * slot fills; reset at the start of each month. Never leave it stale —
 * a fake counter undercuts the whole page.
 */
export const SLOTS_LEFT_THIS_MONTH = 4;

export const CONTACT_EMAIL = "hello@adgraam.com";
